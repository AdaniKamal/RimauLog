import {createClient} from "@supabase/supabase-js";
import {NextResponse} from "next/server";
export async function POST(request:Request){const url=process.env.NEXT_PUBLIC_SUPABASE_URL,key=process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY,serviceKey=process.env.SUPABASE_SERVICE_ROLE_KEY;if(!url||!key||!serviceKey)return NextResponse.json({ok:false},{status:500});const token=request.headers.get("authorization")?.replace(/^Bearer\s+/i,"");if(!token)return NextResponse.json({ok:false},{status:401});const auth=createClient(url,key),{data:{user}}=await auth.auth.getUser(token);if(!user?.email)return NextResponse.json({ok:false},{status:401});const admin=createClient(url,serviceKey,{auth:{autoRefreshToken:false,persistSession:false}});const {error}=await admin.from("invitations").update({accepted_at:new Date().toISOString()}).ilike("email",user.email).is("accepted_at",null);return NextResponse.json({ok:!error},{status:error?400:200})}
