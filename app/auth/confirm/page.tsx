"use client";
import {createClient} from "@supabase/supabase-js";
import {useEffect,useState} from "react";
export default function ConfirmInvitation(){const [message,setMessage]=useState("Verifying your secure invitation…");useEffect(()=>{const url=process.env.NEXT_PUBLIC_SUPABASE_URL,key=process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY;if(!url||!key){setMessage("RimauLog is not configured.");return}const params=new URLSearchParams(window.location.search),token_hash=params.get("token_hash");if(!token_hash){setMessage("This invitation link is incomplete or has already been used.");return}const client=createClient(url,key,{auth:{flowType:"pkce",persistSession:true}});client.auth.verifyOtp({token_hash,type:"invite"}).then(({error})=>{window.history.replaceState(null,"","/auth/confirm");if(error){setMessage(`Invitation could not be verified: ${error.message}`);return}window.location.replace("/")})},[]);return <main className="auth-page"><section className="auth-panel"><div className="auth-brand"><b>R</b><strong>RimauLog</strong></div><span className="auth-kicker">SECURE INVITATION</span><h1>{message}</h1><p>The single-use confirmation value will be removed from your address bar immediately.</p></section><aside><small>PRIVATE MENTORING WORKSPACE</small><blockquote>Welcome to your mentoring journey.</blockquote></aside></main>}
